{% extends "base.html.twig" %}
{% block body %}
<div class="annonceForm_container">
<h1 class="container__title"> Edition de profil </h1>
    {{ form_start(form) }}
    {{ form_row(form.email)}}
    {{ form_row(form.pseudo)}}
    {{ form_row(form.nom)}}
    {{ form_row(form.prenom)}}
    {{ form_row(form.avatar)}}
    {{ form_end(form) }}
</div>
{% endblock %}